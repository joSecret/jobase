---
import '@pcss/components/nav.pcss.css';
import menu from '@data/menu.json';

import HeaderLink from '@/components/ui/HeaderLink.astro';
---

<ul class='nav'>
  {
    menu.main.map((nav) => (
      <li>
        <HeaderLink href={nav.url}>{nav.name}</HeaderLink>
      </li>
    ))
  }
</ul>
